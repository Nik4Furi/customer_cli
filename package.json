{
  "name": "customer-cli",
  "version": "1.0.0",
  "description": "customer-cli , is a command line interface app which can see list ot customers or can do CRUD operations on the customer lists",
  "main": "command.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nik4Furi",
  "license": "ISC",
  "dependencies": {
    "commander": "^9.4.1",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "mongoose": "^6.6.1"
  }
}
